/* random_ints, selection_sort, heap_sort, radix_sort
*  function prototypes
*  for small functions that could be shared between programs
*/

void print_nums(int numbers[], int len);

void swap(int *num1, int *num2);

int max(int nums[], int len);
